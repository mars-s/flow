# Placeholder app icon

`AppIcon.icns` and `AppIconDev.icns` are a flat placeholder mark (no logo,
no wordmark), not a real Flow icon. Replace both with a final design before
shipping. Generate via `iconutil -c icns <name>.iconset` from a 1024x1024
source image (see any macOS icon-set guide for the required sizes).
